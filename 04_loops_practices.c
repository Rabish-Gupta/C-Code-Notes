#include <stdio.h>

int main()
{
    // // sum of first n odd numbers
    // int sum = 0;

    // int num;
    // printf("Enter number of odd numbers :  ");
    // scanf("%d", &num);

    // for (int i = 1, j = 0; j < num; i = i + 2, j++)
    // {
    //     sum = sum + i;
    // }

    // printf("The sum of first %d odd numbers is is %d", num, sum);

    // // to find the number entered is even or odd
    // int poi = 0;

    // int number;
    // printf("Enter your number  :  ");
    // scanf("%d", &number);

    // if (number == 0 || number == 1)
    // {
    //     poi = 1;
    // }

    // for (int i = 2; i < (number / 2); i++)
    // {
    //     if (number % i == 0)
    //     {
    //         poi = 1;
    //         break;
    //     }
    // }

    // if (poi == 1)
    // {
    //     printf("Entered number is composite number.\n");
    // }
    // if (poi == 0)
    // {
    //     printf("Entered number is prime number.\n");
    // }

    // // multiplication table in reverse order

    // int tableNumber;
    // printf("Enter your number : ");
    // scanf("%d", &tableNumber);

    // for (int i = 1; i <= 10; i++)
    // {
    //     printf("%d * %d = %d\n", tableNumber, i, tableNumber * i);
    // }

    // // reverse order
    // printf("*** In reverswe order ***\n");
    // for (int i = 10; i >= 1; i--)
    // {
    //     printf("%d * %d = %d\n", tableNumber, i, tableNumber * i);
    // }

    // // summing the table of a number

    // int sum = 0, i = 0, number;
    // printf("Enter your number :  ");
    // scanf("%d", &number);

    // while (i <= 10)
    // {
    //     sum = sum + number * i;
    //     i++;
    // }

    // printf("The sum of table of %d is  :  %d", number, sum);

    // // factorial of a number

    // int number;
    // int i = 1, factorial = 1;
    // printf("Enter your number :  ");
    // scanf("%d", &number);

    // while (i <= number)
    // {
    //     factorial *= i;
    //     i++;
    // }

    // printf("The factorial of %d is  :  %d", number, factorial);

    return 0;
}